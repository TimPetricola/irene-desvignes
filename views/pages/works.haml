%sections#works
  - @works.each do |slug, work|
    .works-list{:id => slug}
      -work[:images].each do |line|
        - line.each do |image|
          %a{:href => "/images/works/#{slug}/#{image}"}
            %img{:src => "/images/works/#{slug}/thumbs/#{image}"}
        %br